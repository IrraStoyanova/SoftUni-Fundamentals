import java.util.Scanner;

public class Profit {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        int countOneLev = Integer.parseInt(scanner.nextLine());
        int countTwoLev = Integer.parseInt(scanner.nextLine());
        int countThreeLev = Integer.parseInt(scanner.nextLine());
        int amount = Integer.parseInt(scanner.nextLine());


    }
}
